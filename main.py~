#!usr/env/bin/python2.7

import __init__
import sqlite3

def takeattendace():
	

var course = raw_input("Enter the course name.")

if for x in c.execute("SELECT name from course") == course:
	takeattendace()
else:
	var strength=raw_input("Ohh..New Course..Enter the strength of class")
	c.execute("INSERT INTO course VALUES(?,?)",course,strength)


if(course exist):

else:
	var strength=int(raw_input("Enter the strength of student"))
	
	
